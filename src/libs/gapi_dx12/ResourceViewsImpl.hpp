#pragma once

#include "DescriptorHeap.hpp"

namespace RR
{
    namespace GAPI
    {
        // class RenderTargetView;

        namespace DX12
        { /*
            class RenderTargetViewImpl final
            {
            public:
                RenderTargetViewImpl() = default;

                void Init(ID3D12Device* device, const RenderTargetView& rtv, const DescriptorHeap::Descriptor& handle)
                {
                   // handle_()
                }

                D3D12_CPU_DESCRIPTOR_HANDLE inline GetCPUHandle() const
                {
                    return handle.GetCPUHandle();
                }

            private:
                DescriptorHeap::Descriptor handle;
            };*/
        }
    }
}