#include "Math.hpp"

namespace OpenDemo
{
    namespace Common
    {
        void test()
        {
            Vector2 cc(1);
            Vector2 bb(2, 2);
           
            Vector2 asd = cc + bb;
            Vector2 norm = asd.Normal();
            Vector2::ZERO;
        
        }
    }
}